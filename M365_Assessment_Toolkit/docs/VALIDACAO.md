# Validação rápida

1. Execute `python .\scripts\M365-Assessment-Report.py --project-root .` após colocar CSVs em `output/raw`.
2. Confira os arquivos em `output/reports`.
3. Compare os números do Dashboard com os CSVs RAW.
4. Antes de qualquer ação administrativa, valide os achados manualmente no tenant.
